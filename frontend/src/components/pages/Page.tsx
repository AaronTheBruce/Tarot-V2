import React from "react";
import { Container } from "@material-ui/core";

const Page = () => {
  return (
    <Container style={{
      position: 'relative',
      top: '50%',
    }}>
      General Page
    </Container>
  );
};

export default Page;
